"""ExcelTech AI agent business logic, merged into the Flask app.

The legacy FastAPI entrypoint (main.py) is no longer used in production —
all logic now lives in core.py and is invoked directly from backend/app.py.
"""
